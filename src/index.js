import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './css/style.css';
let area = document.getElementById("root");
let root = ReactDOM.createRoot(area);
root.render(<App/>);